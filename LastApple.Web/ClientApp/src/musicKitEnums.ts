export enum PlaybackStates {
    completed,
    ended,
    loading,
    none,
    paused,
    playing,
    seeking,
    stalled,
    stopped,
    waiting,
}

export enum PlaybackBitrate {
    HIGH = 256,
    STANDARD = 64,
}